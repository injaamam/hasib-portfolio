import PageView from "@/components/PageView";
import { pageData } from "@/lib/siteData";

export default function ProfessionalExperiencePage() {
  return <PageView page={pageData.professionalExperience} />;
}
